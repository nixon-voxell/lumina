#import "../packages/suiji-0.3.0/src/lib.typ": *
#import "../monokai_pro.typ": *
#import "../utils.typ": *

#let splash(
  width,
  height,
  hovered_button: none,
  hovered_animation: 0.0,
  time: 0.0,
) = {
  window(
    width,
    height,
  )[ ]
}
